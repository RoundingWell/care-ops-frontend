---
careOptsFrontend:
  components:
    datepicker:
      clear: Clear
      today: Today
      tomorrow: Tomorrow
    droplist:
      defaultText: Choose One...
    selectlist:
      defaultText: Choose One...
      noResultsText: No results
  globals:
    alertBox:
      alertBoxTemplate:
        dismissBtn: Dismiss
        undoBtn: Undo
    error:
      errorTemplate:
        backBtn: Back to Previous Page
        errorHeader: Uh-oh.
        errorInfo: This page doesn't exist.
        errorMessage: Something went wrong.
    modal:
      modalViews:
        modalView:
          cancelText: Cancel
          submitText: Save
  patients:
    list:
      patientsAllViews:
        layoutView:
          groupHeader: Group
          patientHeader: Patient
          title: Patients
    patient:
      dashboard:
        dashboardViews:
          addActionBtn: Action
          dashboardBtn: Dashboard
          dataEventsBtn: Data & Events
          newAction: New Action
      dataEvents:
        dataEventsViews:
          dashboardBtn: Dashboard
          dataEventsBtn: Data & Events
      patientViews:
        contextBackBtn: Back to List
      sidebar:
        patientSidebarGroupsTemplate:
          groupsLabel: Groups
        sidebarViews:
          dob: "{ dob } &ndash; (Age { age })"
          dobLabel: Date of Birth
          mrnLabel: MRN
          sex: |
            {sex, select,
              m {Male}
              f {Female}
              other {}
            }
          sexLabel: Sex
    sidebar:
      actionSidebar:
        createdAt: "Created: <strong>{ createdAt }</strong>"
        updatedAt: "Last Updated: <strong>{ updatedAt }</strong>"
        activityViews:
          created: "<strong>{ first_name } { last_name }</strong> ({ role }) added this Action"
          clinicianAssigned: '<strong>{ first_name } { last_name }</strong> ({ role }) changed the Owner to { to_first_name } { to_last_name }'
          detailsUpdated: '<strong>{ first_name } { last_name }</strong> ({ role }) changed the details of this Action'
          dueDateUpdated: '<strong>{ first_name } { last_name }</strong> ({ role }) changed the Due Date to { date }'
          durationUpdated: '<strong>{ first_name } { last_name }</strong> ({ role }) changed Duration to { duration }'
          nameUpdated: '<strong>{ first_name } { last_name }</strong> ({ role }) changed the name of this Action from { from_name } to { to_name }'
          roleAssigned: '<strong>{ first_name } { last_name }</strong> ({ role }) changed the Owner to { to_role }'
          stateUpdated: '<strong>{ first_name } { last_name }</strong> ({ role }) changed State to { to_state }'
    view:
      viewViews:
        layoutView:
          actionHeader: Action
          attrHeader: State, Owner, Due Date
          patientHeader: Patient
          updatedHeader: Last Updated
        listTitles:
          assignedToMe: Actions Assigned To Me
          delegatedByMe: Actions I've Delegated
locales: en-US
